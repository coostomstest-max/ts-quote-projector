ROLE & OBJECTIVE:
Actúa como un Ingeniero de Software Senior especialista en TypeScript y Arquitectura de Software. Tu tarea es diseñar un plan de refactorización detallado y posteriormente ejecutar los cambios en nuestra base de código para desacoplar la lógica matemática del servicio de negocio, introduciendo además el algoritmo de Suavizado Exponencial Simple (SES).

CONTEXTO DEL SISTEMA:
Actualmente el sistema realiza proyecciones financieras utilizando un modelo de "crecimiento compuesto logarítmico". Sin embargo, esta lógica matemática está acoplada dentro del servicio de negocio principal y queremos migrarla a un módulo de servicios de interpolación puro, junto con el nuevo algoritmo SES.

Estructura de Datos de Entrada:
export type Cotizacion = {
    fecha: string;        // ISO string, ej: "2026-06-05"
    compra: number;
    venta: number;
    currency?: string;    // Opcional
};

-------------------------------------------------------------------------------
TAREA PARTE 1: REFACTORIZACIÓN Y CREACIÓN EN 'interpolation.service.ts'
-------------------------------------------------------------------------------
Este archivo debe funcionar como una librería matemática pura (sin lógica de negocio basada en fechas ni entidades complejas).

1. Extrae la lógica actual de "crecimiento compuesto logarítmico" que reside hoy en el sistema y muévela a un método nuevo y limpio dentro de este archivo. El método debe recibir arrays planos de números y los parámetros estrictamente matemáticos que necesite.
2. Añade el nuevo método core para el algoritmo de Suavizado Exponencial Simple (SES):
   - Nombre sugerido: `calcularSES(datos: number[], alpha: number = 0.3): number`
   - Debe validar que `datos.length > 0`. Si su longitud es 1, retorna ese único valor de inmediato.
   - Aplica la fórmula de recurrencia: Y_hat_{t+1} = (alpha * Y_t) + ((1 - alpha) * Y_hat_t)
   - Inicialización obligatoria: El primer valor suavizado es igual al primer valor real (Y_hat_1 = Y_1).
   - Retorna el valor flotante resultante con máxima precisión (no apliques redondeos dentro del servicio de interpolación).

-------------------------------------------------------------------------------
TAREA PARTE 2: REFACTORIZACIÓN EN 'exchProjector.basic.service.ts'
-------------------------------------------------------------------------------
Este archivo coordina el flujo de negocio y decide qué algoritmo utilizar.

1. Asegúrate de que `InterpolationService` esté correctamente inyectado o disponible en esta clase.
2. Refactoriza por completo el método público o existente `proyectarCotizacion`.
3. Crea un método privado interno (ej: `obtenerOProyectarCotizacion(historial: Cotizacion[], fechaObjetivo: string)`) que será llamado por `proyectarCotizacion` y resolverá la lógica de la siguiente manera:
   - Ordenamiento: Ordena el parámetro 'historial' cronológicamente por el campo 'fecha' de forma ascendente.
   - Búsqueda Directa: Verifica si `fechaObjetivo` ya existe en el historial. Si se encuentra, retorna inmediatamente un objeto `{ compra, venta }` con los valores reales de la base de datos (Bypass del algoritmo).
   - Filtrado de seguridad: Si la fecha NO existe, filtra el historial para conservar únicamente los registros cuya fecha sea ANTERIOR o IGUAL a la `fechaObjetivo`. Esto evita contaminación de datos con información del "futuro".
   - Desacoplamiento de Vectores: Separa los valores en dos arrays planos de números de JavaScript: uno para `compra` y otro para `venta`.
   - Invocación Dinámica: Llama a los métodos correspondientes de `InterpolationService` pasándole estos vectores planos para obtener el cálculo. Debe poder alternar o decidir según el flujo del sistema si utiliza la nueva lógica SES (con un alpha por defecto de 0.3) o el método reubicado de crecimiento compuesto logarítmico.
   - Retorno Uniforme: El método debe retornar un objeto con la estructura estricta: `{ compra: number, venta: number }` calculados para la fecha indicada.

-------------------------------------------------------------------------------
CRITERIOS DE ACEPTACIÓN Y REGLAS DE ORO:
-------------------------------------------------------------------------------
- Preservación de Datos: Ninguno de los métodos modificados o creados debe mutar, alterar o reordenar de forma destructiva los arreglos originales de cotizaciones que ingresan por parámetro.
- Tipado Estricto: Todo el código debe implementarse en TypeScript asegurando que no existan tipos implícitos 'any'.
- Gestión de Errores: Si el historial de cotizaciones está vacío o no cuenta con suficientes datos para realizar los cálculos matemáticos correspondientes, el sistema debe lanzar una excepción controlada clara o manejar un fallback limpio.
- Precisión Flotante: No uses funciones de redondeo como `.toFixed()` dentro del flujo intermedio del bucle para evitar errores acumulativos por propagación.

-------------------------------------------------------------------------------
ENTREGABLE ESPERADO:
-------------------------------------------------------------------------------
Antes de escribir el código definitivo, genera un **Plan de Ejecución paso a paso** detallando qué vas a modificar en cada uno de los archivos (`interpolation.service.ts` y `exchProjector.basic.service.ts`) para que pueda validar tu estrategia de refactorización. Una vez aprobado el plan, procederemos con el código.
