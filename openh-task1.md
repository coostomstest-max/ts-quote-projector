Contexto: Tengo un archivo index.ts con lógica de cotizaciones e iteración de fechas. No hay interfaz gráfica.

Tarea: Construir una UI completa que use esa lógica.

Estructura requerida:
1. Archivo único .html (incluye CSS + JS)
2. Adaptar/convertir el TypeScript a JavaScript dentro del HTML
3. Componentes:
   - Date picker
   - Gráfico (canvas) que muestra proyección según fecha seleccionada
   - Indicadores: valor actual, proyectado, diferencia
4. Evento: onChange del date picker recalcula y actualiza todo

Diseño: moderno, colores suaves, sombras, bordes redondeados, transiciones suaves.

Restricciones: sin frameworks, sin librerías externas, 100% vanilla.